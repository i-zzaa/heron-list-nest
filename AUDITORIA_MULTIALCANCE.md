# Auditoria do Backend — Regras de Negócio MultiAlcance

**Repositório:** `heron-list-nest` (branch `featureGuiaAmil`) · **Escopo:** diagnóstico completo + seis rodadas de correções (ver §9: rodada 1 = bugs mecânicos de baixo risco; rodada 2 = identidade, conflito de agenda/jornada/vínculos, campos bloqueados, recorrência, antecedência de cancelamento e proteção de evento passado; rodada 3 = CORS, migration financeira para `Decimal`, correção do 404 de rota, tratamento de erro real, feriados dinâmicos, seed de cadastros e monitoramento; rodada 4 = autorização por tag nas rotas de maior risco; rodada 5 = migrations formais; rodada 6 = reset de senha seguro, escalada de privilégio, transação real em filas/baixa, snapshot financeiro, exclusão de baixa auditada, exclusão de série bloqueada com sessão realizada, autorização por tag estendida a agenda/demais cadastros, exclusão de cadastro em uso, segredos sem fallback hardcoded).
**Data:** 2026-08-04 (atualizado em 2026-08-05)

---

## Sumário

1. [Resumo executivo](#1-resumo-executivo)
2. [Arquitetura encontrada](#2-arquitetura-encontrada)
3. [Matriz de conformidade](#3-matriz-de-conformidade)
4. [Problemas encontrados](#4-problemas-encontrados)
5. [Regras somente no frontend](#5-regras-somente-no-frontend)
6. [Endpoints encontrados (amostra)](#6-endpoints-encontrados-amostra)
7. [Modelo de dados](#7-modelo-de-dados)
8. [Testes](#8-testes)
9. [Ajustes já aplicados nesta sessão](#9-ajustes-já-aplicados-nesta-sessão)
10. [Plano de correção (etapas restantes)](#10-plano-de-correção-etapas-restantes)
11. [Decisões de negócio pendentes](#11-decisões-de-negócio-pendentes)

---

## 1. Resumo executivo

**Percentual estimado de atendimento às regras descritas: ~70–75%** (era ~25–30% no diagnóstico original). A rodada 6 fechou os maiores buracos de segurança (senha previsível, escalada de privilégio, autorização por tag restrita a poucas rotas) e de integridade (transação real em filas/baixa, snapshot financeiro, exclusão de série sem checar sessão já realizada, exclusão de cadastro em uso). O que resta pendente está listado em §10/§11 e depende, em boa parte, de decisão de negócio (ex.: rotação de credenciais, dados de `GrupoPermissaoOnPermissao`).

> **Correção em relação à primeira versão deste relatório:** a regra "terapeuta só possui uma especialidade" **é intencional**, não é uma falha. O modelo `Terapeuta.especialidadeId` (FK única) está correto. O que a terapeuta pode ter em quantidade é **função** dentro dessa especialidade — e isso já está modelado corretamente via `TerapeutaOnFuncao` (N:N entre terapeuta e função, com comissão por função). O item antigo "R23 — terapeuta com só 1 especialidade" foi **removido da lista de problemas**.

| Módulo | Situação |
|---|---|
| Autenticação/Autorização | ✅ Quase completo — identidade via JWT verificado; `VagaController` com guard; **autorização por tag agora cobre usuários, grupo-permissão, baixa, paciente, agenda e os 6 cadastros auxiliares restantes** (frequência, função, localidade, modalidade, programa, status-eventos — rodada 6). Sem tag no catálogo para periodo/perfil/status/tipo-sessão/convênio/sessão/especialidade (não existem esses tags) e para `/vagas/*` (ação não mapeia 1:1 pra uma tag) — seguem só com `AuthGuard('jwt')`, documentado |
| Cadastro de terapeuta (jornada, comissão, especialidade/funções) | ⚠️ Parcial — 1 especialidade + N funções está correto; jornada 8h–20h/sem sobreposição agora é validada no agendamento (corrigido) |
| Paciente | ⚠️ Parcial — sem dedupe de carteirinha (dados reais têm duplicidade, decisão de limpeza pendente — §11); snapshot financeiro agora existe (rodada 6); `delete()` apagava a entidade errada (corrigido) |
| Filas (avaliação/devolutiva/terapia) | ✅ `VagaService.update()` agora roda inteiro dentro de um `prisma.$transaction` real (rodada 6) — atomicidade de verdade entre `VagaOnEspecialidade`, `Vaga`, `Paciente` e `PacienteHistorico`, não mais `Promise.all` solto. De brinde, dois pontos que retornavam uma `Promise` sem `await` (bug real, quebrava a atomicidade) foram corrigidos |
| Agenda | ✅ Conflito de horário, jornada e vínculo especialidade/terapeuta/função agora validados na criação e edição; evento passado agora bloqueia edição de campos (só status) e exclusão; exclusão de série agora bloqueada se qualquer ocorrência já foi realizada (rodada 6) |
| Edição "esta e as próximas" | ✅ Corrigido — `changeAll` agora sempre passa pelo split correto (série antiga trunca, série nova nasce da data atual); campos travados (modalidade/data/horário/frequência/intervalo/dias) são sempre mantidos do original |
| Cancelamento com/sem antecedência | ✅ Corrigido — backend decide sozinho (48h corridas até o início do evento), ignorando o status enviado pelo cliente quando for um dos dois de cancelamento |
| Baixa | ✅ Quase completo — evita baixa duplicada (sinalizada), `usuarioId` vem do JWT, update do evento + criação da baixa numa transação real (rodada 6), exclusão agora exige motivo e grava snapshot em `BaixaExclusaoLog` antes de apagar fisicamente (rodada 6). Falta só `@@unique(eventoId)` a nível de banco — na real, já existe (ver R15) |
| Financeiro | ✅ Snapshot implementado (rodada 6) — valor da sessão, comissão, tarifa de km e de devolutiva agora são gravados no evento no momento em que paciente/especialidade/terapeuta/função são definidos, e `FinanceiroService` passa a preferir esse valor congelado em vez de reler o cadastro atual. Populado retroativamente nos eventos que já existiam ([prisma/backfill-financeiro-snapshot.ts](prisma/backfill-financeiro-snapshot.ts)) |
| CRUD de cadastros (13 controllers) | ✅ Corrigido — endpoints `DELETE /:id` estavam todos quebrados por bug de binding do NestJS; exclusão de Especialidade/Função/Localidade/Status de Evento agora bloqueada com mensagem clara quando a entidade está em uso, em vez de erro cru de FK (rodada 6) |
| Tratamento de erro (`responseError`) | ✅ Corrigido — status e mensagem reais por tipo de erro (HttpException/Prisma/Error/string), nunca mais 401 fixo pra tudo, nunca vaza stack trace |
| Rota `/especialidade` (e prefixo `/api`) | ✅ Corrigido — faltava `setGlobalPrefix('api')` e o endpoint `GET` paginado inteiro não existia nesse controller |
| Monitoramento | ✅ Novo — log estruturado de toda requisição/erro (interceptor + filtro globais) |
| Segredos (`JWT_PRIVATE_KEY`/sessão) | ✅ Corrigido (rodada 6) — fallback hardcoded `'dev-secret-key'` removido de `JwtStrategy` e da sessão; app falha explicitamente no boot se a env var não estiver configurada. Sessão passou a usar `SESSION_PRIVATE_KEY` (dedicada, já existia no `.env` mas nunca era lida) em vez de reaproveitar `JWT_PRIVATE_KEY` |
| Testes | ✅ Melhorou bastante — 119 testes (31 originais + 88 novos entre as seis rodadas desta sessão, 46 só na rodada 6), 18 suites; ainda sem e2e, sem teste de concorrência real (só de atomicidade via mock de transação) |
| Migrations/Seeds | ✅ Corrigido — seed de 18 tabelas de cadastro (real) **e** histórico de migrations formal via `prisma migrate` (`prisma/migrations/`: baseline + 2 migrations reais); script `migrate:deploy` cadastrado em `package.json` (rodada 6) |
| Multiclínica | ❌ Não existe (não há `Clinica`/tenant em nenhuma entidade) — informativo, decisão de negócio sobre ser requisito futuro segue em aberto |

### Riscos críticos (bloqueadores de produção)

1. ~~**Escalada de privilégio**: qualquer usuário autenticado podia criar/editar usuários, grupos de permissão e resetar a senha de qualquer conta~~ — **corrigido**: `PermissionsGuard` + tags reais do banco (`CADASTRO_USUARIOS_*`, `CADASTRO_GRUPO_PERMISSOES_*`) exigidas nessas rotas; `grupoPermissaoId` saiu de `POST/PUT /usuarios` e virou rota dedicada (`PUT /usuarios/:id/grupo-permissao`, mesma tag de administrar grupo); atribuir perfil Developer (bypass total) só é permitido para quem já é Developer; reset de senha agora gera senha aleatória com troca obrigatória no próximo login, em vez de `'12345678'` fixo (rodada 6). **Ressalva que permanece**: os grupos `RECEPCAO`/`RECEPCAO BASICO` têm hoje as mesmas tags perigosas que `ADM` no banco real — é dado (`GrupoPermissaoOnPermissao`), não bug de código, e segue sem decisão sua (ver §9 rodada 4, §11 item 21).
2. ~~**Identidade de negócio via header HTTP livre** (`req.headers.login`)~~ — **corrigido**: `agenda`, `usuarios`, `sessao` e `baixa` agora usam `req.user.username`, populado pelo payload assinado do JWT (`AuthGuard('jwt')`), não mais um header que o cliente controla.
3. ~~**`VagaController` sem autenticação nenhuma**~~ — **corrigido** (guard reativado). Antes da correção, qualquer pessoa na internet, sem token, conseguia mover pacientes entre filas.
4. ~~**Nenhuma verificação de conflito de horário** ao criar/editar eventos~~ — **corrigido**: dupla marcação, sobreposição parcial e evento fora da jornada (8h–20h ou fora do dia cadastrado) agora são rejeitados na criação e em toda edição que troca a terapeuta.
5. ~~**Bug de exclusão cruzada**: `PacienteService.delete()` deletava um registro de `Localidade`, não o paciente~~ — **corrigido**.
6. ~~**Todos os 13 endpoints `DELETE /:id` de cadastro**~~ — **corrigido**.
7. ~~**Ausência de transação real (`prisma.$transaction`) nos fluxos de fila**~~ (`VagaService.update`) — **corrigido** (rodada 6): todo o método passou a rodar dentro de um `prisma.$transaction(async (tx) => {...})` real, com o client transacional propagado por `PacienteService`/`VagaService` (`setStatusPaciente`, `setTipoSessaoTerapia`, `verifyInFila`, `removeEvents`, `setQueueStatus`) — não é mais só concorrência via `Promise.all`, é atomicidade de fato. A mesma lógica foi aplicada ao update do evento + criação da baixa decorrente em `AgendaService` (4 pontos).
8. ~~**Financeiro sem precisão decimal / valores em `String`**~~ — **corrigido**: `VagaOnEspecialidade.valor`/`.km`, `TerapeutaOnFuncao.comissao`, `Calendario.km` migrados para `Decimal(10,2)` no banco real (dados existentes normalizados antes, sem perda). ~~**Sem snapshot**~~ — **corrigido** (rodada 6): valor/comissão/tarifas agora são congelados no evento quando paciente/especialidade/terapeuta/função são definidos; `FinanceiroService` prefere esse valor ao cadastro atual; eventos que já existiam foram populados retroativamente.
9. ~~**"Esta e as próximas" reescrevia ocorrências passadas**~~ — **corrigido**: `changeAll` agora sempre passa pela lógica de split (trunca a série antiga, cria a nova a partir da data atual) em vez de um `updateMany` cru por `groupId`.
10. ~~**Campos bloqueados (modalidade/data/horário/frequência/intervalo/dias) eram graváveis via update**~~ — **corrigido**: sempre mantidos do registro original, ignorados silenciosamente se vierem diferentes no payload.
11. ~~**Cliente escolhia livremente entre "Cancelado com Antecedência" e "Cancelado sem Antecedência"**~~ — **corrigido**: backend recalcula com base em 48h corridas até o início do evento.
12. ~~**Evento passado podia ser editado/excluído livremente**~~ — **corrigido**: exclusão bloqueada; edição só aceita status = Atestado, com tolerância de 2h após o término (para não quebrar o check-in mobile).
13. ~~**`responseError` sempre 401 pra tudo**~~ — **corrigido**: status e mensagem reais por tipo de erro.
14. ~~**`GET /especialidade` (e toda rota, na real) respondendo 404**~~ — **corrigido**: faltava `setGlobalPrefix('api')` e faltava o endpoint `getAll` inteiro em `EspecialidadeController`.
15. ~~**`Baixa.create` silenciosa em duplicidade**~~ — **corrigido**: agora loga e sinaliza no retorno.
16. ~~**`perfilId` sem validação**~~ — **corrigido**: valida contra a tabela `Perfil`.
17. ~~**Sem seed**~~ — **parcialmente corrigido**: seed de 18 tabelas de cadastro real criado (`prisma/seed.ts`), PII e agendamento/filas excluídos por decisão própria (ver §11).
18. ~~**Sem log estruturado / monitoramento**~~ — **corrigido**: interceptor + filtro globais logam toda requisição e erro.

---

## 2. Arquitetura encontrada

- **Framework:** NestJS 9, TypeScript 4.7, sem `strictNullChecks`/`noImplicitAny`.
- **ORM:** Prisma 4.9 (`prisma/schema.prisma`).
- **Banco:** MySQL.
- **Autenticação:** Passport (`passport-local` no login, `passport-jwt` nas rotas) + `express-session`/`passport.session()` coexistindo com JWT stateless sem necessidade aparente.
- **Autorização:** inexistente além de "token válido" — sem guard de tags/permissão.
- **Padrão arquitetural:** Controller → Service → Prisma direto, sem repository, sem DTOs de classe validados (`*.interface.ts` são apenas `interface`/`enum`, não `class` com `class-validator`); `ValidationPipe` global está registrado mas não tem o que validar.
- **Migrations:** versionadas via `prisma migrate` — `prisma/migrations/` com uma migration baseline (`20260805110114_init`, marcada como já aplicada, sem executar nada) e duas migrations reais: `20260805123719_baixa_exclusao_log` (cria a tabela `BaixaExclusaoLog`, hoje conectada ao `schema.prisma` e usada por `BaixaService.delete()` — R16, rodada 6) e `20260805165751_rodada6_seguranca_financeiro` (`Usuario.mustChangePassword`, snapshot financeiro em `Calendario`, `@unique`/`@@index` novos). Script `npm run migrate:deploy` cadastrado em `package.json` (rodada 6).
- **Seeds:** criado nesta sessão — [prisma/seed.ts](prisma/seed.ts), 18 tabelas de cadastro/referência com dados reais atuais, PII e agendamento/filas excluídos (ver §9 rodada 3, §11).
- **Testes:** 8 suites / 31 testes unitários no diagnóstico original (hoje 119, em 18 suites, após os testes adicionados ao longo da sessão — ver §8), todos passam; nenhum e2e (pasta `test/` inexistente apesar do script `test:e2e` referenciá-la).
- **Build/lint:** `tsc --noEmit` limpo; `nest build` sem erros; `eslint` com 35 erros de formatação e 71 warnings de variável não usada — não bloqueante.
- **CORS:** corrigido nesta sessão — configuração única, sem o bug de origem com `/` no final.
- **Prefixo de rota:** corrigido nesta sessão — `setGlobalPrefix('api')` (não existia; toda rota respondia sem `/api`, causando 404 no frontend).
- **Tratamento de erro:** corrigido nesta sessão — `responseError` resolve status/mensagem reais por tipo de erro; filtro global de exceções cobre o que escapa do try/catch de controller.
- **Monitoramento:** novo nesta sessão — log estruturado de requisição/erro via interceptor + filtro globais.
- **Segredo JWT/sessão:** ~~fallback hardcoded `'dev-secret-key'`~~ — **corrigido** (rodada 6): `JwtStrategy` lança erro no boot se `JWT_PRIVATE_KEY` não estiver definido; a sessão passou a usar `SESSION_PRIVATE_KEY` (dedicada, já existia no `.env` sem ser lida) em vez de reaproveitar `JWT_PRIVATE_KEY`, com o mesmo fail-fast.
- **Swagger:** não presente no projeto.

---

## 3. Matriz de conformidade

| ID | Área | Regra | Status | Evidência | Problema | Severidade | Correção |
|----|------|-------|--------|-----------|----------|------------|----------|
| R1 | Segurança | Guard de permissão por tag protegendo rotas | **ATENDE (corrigido, com resíduo documentado)** | `PermissionsGuard` + `@RequirePermission('cod')`, aplicados em usuários, grupo-permissões, baixa, paciente (rodada 4) **e agora também** `POST/PUT/DELETE /evento`, frequência, função, localidade, modalidade, programa, status-eventos (rodada 6, 33 rotas novas guardadas) | Periodo/perfil/status/tipo-sessão/convênio/sessão/especialidade não têm tag correspondente no catálogo — seguem sem tag. `/vagas/agendar`/`/vagas/devolutiva` não mapeiam 1:1 pra uma tag (o mesmo endpoint atende avaliação/devolutiva/terapia) — deixados sem tag por decisão de manter o mesmo rigor da rodada 4 (só guardar onde o mapeamento é inequívoco) | **Crítico** | ✅ Corrigido nas rotas com tag disponível e mapeamento claro (§9 rodadas 4 e 6); resíduo é limitação do catálogo de tags, não do código |
| R2 | Segurança | Identidade do usuário vem do JWT verificado | **ATENDE (corrigido)** | `agenda`, `usuarios`, `sessao`, `baixa` agora usam `req.user?.username`, preenchido pelo Passport a partir do payload assinado do JWT | — | — | ✅ Corrigido (§9 item 7/8) |
| R3 | Segurança | Reset de senha exige permissão e não é previsível | **ATENDE (corrigido)** | `GET /usuarios/reset-senha/:id` exige `CADASTRO_USUARIOS_LISTA_BOTAO_RESETAR_SENHA`; `updatePassword`/`create` agora geram senha aleatória (`generateRandomPassword`, sem caracteres ambíguos) e marcam `Usuario.mustChangePassword = true` — devolvida em texto plano só na resposta pra quem tem a permissão | `PermissionsGuard` bloqueia qualquer rota com tag enquanto `mustChangePassword` estiver pendente (mesmo pra Developer); rotas de troca da própria senha não têm tag, então continuam acessíveis pra sair desse estado | — | ✅ Corrigido (§9 rodada 6) — decisão de negócio: senha aleatória + troca obrigatória no 1º login |
| R4 | Segurança | Escalada de privilégio bloqueada | **ATENDE (corrigido)** | `POST/PUT /usuarios` exigem tag; `grupoPermissaoId` saiu do payload dessas rotas — vira `PUT /usuarios/:id/grupo-permissao`, rota dedicada com a tag de administrar grupo (`CADASTRO_GRUPO_PERMISSOES_LISTA_BOTAO_EDITAR`); atribuir perfil Developer exige que quem chama já seja Developer | `perfilId` continua editável nas rotas normais de usuário (é acoplado à gestão de Terapeuta — criar/editar um usuário como terapeuta precisa saber o perfil no mesmo payload), mas não carrega mais poder de bypass a não ser o caso Developer, que agora tem guard próprio | **Alto** | ✅ Corrigido (§9 rodada 6) — decisão de negócio: rota separada só para quem administra grupos |
| R4b | Segurança | Todas as rotas de negócio exigem autenticação | **INCORRETO (corrigido)** | `vaga.controller.ts:16` tinha `@UseGuards(AuthGuard('jwt'))` **comentado** | Fila de pacientes (`/vagas/agendar`, `/vagas/devolutiva`) ficava 100% pública | **Crítico** | ✅ Guard reativado nesta sessão |
| R5 | Segurança | 401 vs 403 diferenciados | **ATENDE (corrigido)** | `resolveError` em `response.ts`, usado por `responseError` e por `AllExceptionsFilter` | Status resolvido pelo tipo real do erro (HttpException/Prisma/Error/string); nunca vaza stack trace | — | ✅ Corrigido (§9 item 25) |
| R6 | Agenda | Conflito de horário do mesmo terapeuta é rejeitado | **ATENDE (corrigido)** | `hasScheduleConflict` em `agenda.service.ts`, chamado no create e em toda edição que muda a terapeuta | Cobre evento único × único, único × recorrente e recorrente × recorrente (materializando datas com `getDates`, teto de 365 dias) | — | ✅ Corrigido (§9 item 13) — falta teste de concorrência real (duas requisições simultâneas, ver R12) |
| R7 | Agenda | Evento respeita jornada da terapeuta (8h–20h, dias trabalhados) | **ATENDE (corrigido)** | `validateJornada` em `agenda.service.ts` | Rejeita fora de 08:00–20:00, `start >= end` e dia/horário fora do `cargaHoraria` cadastrado | — | ✅ Corrigido (§9 item 12) |
| R8 | Agenda | Editar "esta e as próximas" não altera ocorrências passadas | **ATENDE (corrigido)** | `updateCalendario` não tem mais o atalho de `updateMany({where:{groupId}})`; sempre passa por `updateEventoRecorrentesAllChange`, que faz o split (trunca a antiga, cria a nova a partir da data atual) | — | ✅ Corrigido (§9 item 9) |
| R9 | Agenda | Campos bloqueados após criação (modalidade, data, horário, frequência, intervalo, dias) | **ATENDE (corrigido)** | `formatEvent(event, original)` mantém esses campos sempre do registro original, ignorando o payload | — | ✅ Corrigido (§9 item 10) |
| R10 | Agenda | Exclusão/edição de evento passado é bloqueada, com Atestado permitido | **ATENDE (corrigido)** | `isEventoPassado`, `assertSomenteStatusAlterado`, `assertStatusPermitidoParaEventoPassado` em `agenda.service.ts` | `delete()` rejeita evento já ocorrido; edição só aceita mudar `statusEventosId`, e só para **Atestado**; tolerância de 2h após o término (`TOLERANCIA_EVENTO_PASSADO_HORAS`) para não travar o check-in mobile | ✅ Corrigido (§9 itens 15, 17, 18) |
| R11 | Agenda | Exclusão de série (esta / esta+próximas / toda) definida | **ATENDE (regra fechada com o negócio)** | `delete()` agora bloqueia a exclusão da série inteira se **qualquer ocorrência** dela (mesmo de outra linha da mesma série, via split `isChildren`) já foi realizada — só permite excluir séries totalmente futuras | Não existe granularidade "só esta ocorrência"/"esta e as próximas" (a regra fechada foi mais restritiva que isso: bloqueia tudo se tiver algo passado, não oferece exclusão parcial) — se precisar dessa granularidade fina no futuro, é uma regra nova, não um bug | — | ✅ Corrigido (§9 rodada 6) — decisão de negócio: só exclui série 100% futura |
| R12 | Agenda | Transação nos fluxos de update/create de eventos | **ATENDE (corrigido)** | Os 4 pontos que fazem update/create de `Calendario` + criação condicional de `Baixa` (`updateCalendario`, `updateEventoUnicoGrupo`, `updateEventoRecorrentes`, `updateEventoRecorrentesAllChange`) agora rodam dentro de `prisma.$transaction(async (tx) => {...})` | Se a baixa falhar, o update do evento é revertido junto — atomicidade real, não só `await` sequencial | — | ✅ Corrigido (§9 rodada 6) |
| R13 | Filas | Transição avaliação→devolutiva→terapia é transacional e idempotente | **ATENDE (corrigido)** | `VagaService.update()` inteiro roda dentro de `prisma.$transaction`, com o client transacional propagado por `PacienteService`/`VagaService`; `case queue_avaliation` com `break` (corrigido rodada 1); dois pontos que retornavam uma `Promise` sem `await` (bug real que também quebrava a atomicidade) corrigidos junto | — | — | ✅ Corrigido (§9 rodada 6) |
| R14 | Filas | Fila é modelada por entidade/estado explícito | **ATENDE PARCIALMENTE** | `Vaga` + `VagaOnEspecialidade` fazem o papel de máquina de estados, sem histórico tipado | Difícil auditar transições | **Médio** | Formalizar histórico |
| R15 | Baixa | Um evento não recebe duas baixas ativas | **ATENDE (corrigido)** | `baixa.service.ts create()` verifica antes de criar e loga/sinaliza (`{duplicate:true}`) em vez de retornar `undefined` em silêncio; `Baixa.eventoId` ganhou `@@unique` no banco real (rodada 6, sem duplicidade encontrada nos dados) | A checagem em código (`findMany` antes do `create`) ainda tem uma pequena janela de corrida em teoria, mas agora o `@@unique` do banco é quem garante de verdade — uma segunda tentativa simultânea falharia na constraint em vez de criar duplicata | — | ✅ Corrigido (§9 rodada 6) |
| R16 | Baixa | Exclusão de baixa preserva histórico/motivo | **ATENDE (corrigido)** | `delete()` em [src/baixa/baixa.service.ts](src/baixa/baixa.service.ts) agora exige `motivo`, resolve o usuário autenticado, grava snapshot completo da baixa + motivo + usuário em `BaixaExclusaoLog` (modelo conectado ao `schema.prisma`) numa transação, só então apaga fisicamente a `Baixa` | Continua sendo exclusão física da linha em `Baixa` (não soft delete) — o que R16 pedia era não perder motivo/histórico, e isso está em `BaixaExclusaoLog` agora | — | ✅ Corrigido (§9 rodada 6) — infraestrutura que já existia solta desde a rodada 5 foi conectada |
| R17 | Financeiro | Snapshot do valor/comissão no momento do evento | **ATENDE (corrigido)** | `Calendario` ganhou `valorSessaoSnapshot`/`comissaoSnapshot`/`tipoComissaoSnapshot`/`valorPorKmSnapshot`/`valorSessaoDevolutivaSnapshot`, calculados e gravados sempre que paciente/especialidade/terapeuta/função são definidos (criação ou edição que muda algum desses); `FinanceiroService` prefere esses campos, com fallback pro cadastro atual só se `null` | Congelado no momento do evento, não da baixa (mais simples e cobre também eventos sem baixa); populado retroativamente nos registros que já existiam ([prisma/backfill-financeiro-snapshot.ts](prisma/backfill-financeiro-snapshot.ts)) | — | ✅ Corrigido (§9 rodada 6) — decisão de negócio: congelar e popular retroativamente |
| R18 | Financeiro | Precisão monetária (Decimal) | **ATENDE (corrigido)** | `VagaOnEspecialidade.valor`/`.km`, `TerapeutaOnFuncao.comissao`, `Calendario.km` agora `Decimal(10,2)` no schema **e no banco real** (confirmado via `information_schema`); leitura/escrita via `readDecimal`/`normalizeCurrencyValue` | Dados existentes normalizados antes da migration (havia NBSP + vírgula decimal) | — | ✅ Corrigido (§9 item 20/21) |
| R19 | Financeiro | Valor por km e valor de devolutiva configuráveis | **ATENDE (corrigido)** | `FINANCEIRO_VALOR_POR_KM`/`FINANCEIRO_VALOR_SESSAO_DEVOLUTIVA` em `financeiro.service.ts` | Configurável via env var, default igual ao valor antigo | — | ✅ Corrigido (§9 item 22) |
| R20 | Cadastros | Exclusão bloqueada quando entidade está em uso | **ATENDE (corrigido)** | Novo helper [src/util/assert-not-in-use.ts](src/util/assert-not-in-use.ts), usado em `delete()` de Especialidade (evento/terapeuta/vaga/função), Função (evento/terapeuta), Localidade (evento/baixa) e StatusEventos (evento/baixa) | Checa uso antes de excluir e lança erro com mensagem clara (`responseError` já resolve pra 400) em vez do 500 genérico de FK; continua sendo hard delete quando **não** está em uso (não virou `ativo=false` automático) | — | ✅ Corrigido (§9 rodada 6) |
| R21 | Cadastros | Endpoints `DELETE /:id` funcionam | **INCORRETO (corrigido)** | 13 controllers usavam `@Param() id: number` (sem chave) → `id` virava o objeto `{id:'...'}` inteiro → `Number(id)` = `NaN` em todos os services | **Todo** endpoint de exclusão de cadastro estava quebrado (paciente, especialidade, função, localidade, status-evento, modalidade, período, perfil, status, tipo-sessão, convênio, frequência, sessão) | **Alto** | ✅ Corrigido nesta sessão (`@Param('id')` em todos) |
| R22 | Cadastros | `PacienteService.delete()` remove o paciente | **INCORRETO (corrigido)** | `delete(id)` executava `prisma.localidade.delete(...)` — apagava uma **Localidade**, não o Paciente | Bug de corrupção de dados | **Crítico** | ✅ Corrigido nesta sessão — agora inativa o paciente (`disabled:true`), preservando histórico |
| R23 | Terapeuta | Terapeuta tem uma especialidade e várias funções dentro dela | **ATENDE** | `Terapeuta.especialidadeId` (FK única) + `TerapeutaOnFuncao` (N:N terapeuta↔função) | — regra confirmada pelo negócio, modelo já correto | — | Nenhuma (item removido da lista de problemas) |
| R24 | Terapeuta | Jornada 8h–20h validada no backend, sem sobreposição | **ATENDE (corrigido)** | Mesma implementação do R7 (`validateJornada`) — terapeuta só tem 1 jornada, não há "sobreposição entre terapeutas" aplicável aqui, sobreposição de horário é o R6 | — | — | ✅ Corrigido (§9 item 12) |
| R25 | Terapeuta | Perfil de usuário validado por enum fixo (Admin/Coordenadora/Terapeuta/Secretária) | **ATENDE PARCIALMENTE** | `validatePerfilId()` valida contra a tabela; conferido no banco real (`SELECT id, nome FROM Perfil`) que só existem 5 linhas — Developer + os 4 papéis do enunciado — e `PERFIL` em `src/util/util.ts` agora lista as 5 como enum de código (rodada 6), usado para a checagem "só Developer atribui Developer" (R4) | `Perfil` continua tabela livre no banco (não virou enum de coluna — mudar o tipo da coluna é uma migration mais arriscada, e a validação de existência já cobre o essencial); `ID_PERFIL_TERAPEUTA.id = 5` continua um ID mágico hardcoded, não removido | **Médio** | ✅ Validação de existência (§9 item 27) e enum de código com a lista real (§9 rodada 6); enum de banco e remoção do ID mágico seguem não feitos, por decisão técnica de risco/benefício |
| R26 | Vagas/Filas | Vínculo especialidade↔paciente/terapeuta validado no agendamento | **ATENDE (corrigido)** | `validateAgendamentoVinculos` em `agenda.service.ts` | Rejeita especialidade que a terapeuta não tem, função fora da especialidade, terapeuta sem a função, especialidade não vinculada ao paciente — mesmo com IDs manuais | — | ✅ Corrigido (§9 item 11) |
| R27 | Cancelamento | Antecedência mínima calculada e status escolhido automaticamente | **ATENDE (corrigido)** | `resolveStatusCancelamento` em `agenda.service.ts` | Backend recalcula com 48h corridas até o início do evento sendo alterado; substitui silenciosamente o status enviado quando for um dos dois de cancelamento | — | ✅ Corrigido (§9 item 14) |
| R28 | Migrations/Seeds | Migrations versionadas e seeds dos cadastros mínimos | **ATENDE (corrigido)** | [prisma/seed.ts](prisma/seed.ts) (18 tabelas, idempotente) + `prisma/migrations/` com baseline e 2 migrations reais via `prisma migrate`, não mais `db push`; script `migrate:deploy` em `package.json` | — | — | ✅ Seed criado (§9 rodada 3); migrations formalizadas (§9 rodada 5); script + 2ª migration real (§9 rodada 6) |
| R29 | Multiclínica | Isolamento por clínica/tenant | **NÃO ATENDE** | Nenhuma entidade tem campo de tenant | Não aplicável hoje (mono-clínica) | Informativo | Definir se é requisito futuro |
| R30 | Consulta de agenda | API consulta só o período pedido | **ATENDE** | `getFilter`/`getRange` usam `buildDateRangeWhere` | — | — | — |
| R31 | Paginação | Tamanho de página tem limite máximo no backend | **ATENDE (corrigido)** | `normalizePageSize` em `src/util/pagination.ts`, aplicado nos 13 controllers paginados | Teto de 100 itens por página | — | ✅ Corrigido (§9 rodada 1 item 6) |
| R32 | Agenda | Evento passado só aceita status Atestado | **ATENDE (corrigido)** | `assertStatusPermitidoParaEventoPassado` em `agenda.service.ts` | Definição fechada com o negócio; qualquer outro status é rejeitado | — | ✅ Corrigido (§9 rodada 2 item 17) |
| R33 | Agenda | Tolerância de 2h após o término antes de considerar o evento "passado" | **ATENDE (corrigido)** | `TOLERANCIA_EVENTO_PASSADO_HORAS` em `isEventoPassado`, `agenda.service.ts` | Evita quebrar o check-in mobile (`PUT /evento/check`, marca Atendido), que roda perto do fim da sessão | — | ✅ Corrigido (§9 rodada 2 item 18) |
| R34 | Cadastros | Feriados nacionais usados no cálculo de disponibilidade/dia útil | **ATENDE (corrigido)** | `calcularFeriadosNacionais()` em `format-date.ts` | Antes era lista fixa só até 2022 (e com bug de formato de data que a deixava sem efeito nenhum); agora calcula fixos + móveis (Páscoa) para janela de anos ao redor de hoje. Não cobre feriado municipal/estadual | — | ✅ Corrigido (§9 rodada 3 item 28) |
| R35 | Segurança | Log estruturado de requisição/erro | **ATENDE (corrigido)** | `LoggingInterceptor` + `AllExceptionsFilter`, globais em `main.ts` | — | — | ✅ Novo (§9 rodada 3 item 31) |

---

## 4. Problemas encontrados

### Críticos — ainda abertos
Nenhum. Os 6 itens que estavam aqui (R1 autorização por tag, R3 senha previsível, R4 mass assignment, R13 transação em fila, R17 snapshot financeiro, R11 exclusão de série) foram todos corrigidos na rodada 6 — ver lista abaixo e §9.

### Críticos — corrigidos nesta sessão
- ~~`VagaController` sem autenticação nenhuma~~ — corrigido (R4b).
- ~~Identidade via header `login` falsificável~~ — corrigido (R2).
- ~~Nenhuma checagem de conflito de horário~~ — corrigido (R6).
- ~~Evento fora da jornada da terapeuta era aceito~~ — corrigido (R7/R24).
- ~~`changeAll` reescrevia ocorrências passadas~~ — corrigido (R8).
- ~~Campos bloqueados (modalidade/data/horário/frequência/intervalo/dias) graváveis via update~~ — corrigido (R9).
- ~~`switch` sem `break` em `VagaService.update`~~ — corrigido (R13).
- ~~`PacienteService.delete()` apagava uma `Localidade`~~ — corrigido (R22).
- ~~Vínculo especialidade/terapeuta/função não validado no `createCalendario`~~ — corrigido (R26).
- ~~Regra de antecedência de cancelamento não existia no backend~~ — corrigido, 48h (R27).
- ~~Evento passado editável/excluível livremente~~ — corrigido: exclusão bloqueada, edição só aceita status = Atestado, com tolerância de 2h (R10, R32, R33).
- ~~Valores monetários como `String` (risco de corrupção/parsing quebradiço)~~ — corrigido: migrado para `Decimal(10,2)` no banco real (R18).
- ~~`responseError` sempre 401 pra tudo~~ — corrigido: status/mensagem reais por tipo de erro (R5).
- ~~`GET /especialidade` (e toda rota) respondendo 404~~ — corrigido: faltava `setGlobalPrefix('api')` e o endpoint `getAll` inteiro.
- ~~`Baixa.create` silenciosa em duplicidade~~ — corrigido: agora loga e sinaliza (R15, ainda sem `@@unique` de banco).
- ~~`perfilId` sem validação~~ — corrigido: valida contra a tabela `Perfil` (R25, ID mágico segue, enum de código agora existe).
- ~~CORS redundante/conflitante~~ — corrigido: configuração única, origem de produção com bug de `/` corrigido.
- ~~Sem autorização por permissão~~ (R1) — **corrigido** na maioria das rotas com tag disponível (§9 rodadas 4 e 6).
- ~~Reset de senha sem permissão, para senha fixa conhecida~~ (R3) — **corrigido**: senha aleatória + troca obrigatória (§9 rodada 6).
- ~~Mass assignment de `grupoPermissaoId`/`perfilId`~~ (R4) — **corrigido**: rota separada + guard de perfil Developer (§9 rodada 6).
- ~~Ausência de transação real nos fluxos de fila~~ (R13) — **corrigido**: `$transaction` real (§9 rodada 6).
- ~~Financeiro sem snapshot~~ (R17) — **corrigido**: valor/comissão/tarifas congelados no evento (§9 rodada 6).
- ~~Exclusão de série recorrente sem checar sessão já realizada~~ (R11) — **corrigido**: bloqueia se qualquer ocorrência já passou (§9 rodada 6).

### Altos
- ~~Exclusão de baixa é física, sem motivo/auditoria~~ (R16) — **corrigido**: exige motivo, grava snapshot em `BaixaExclusaoLog` antes de apagar (§9 rodada 6).
- ~~Ausência de migrations formais (`prisma migrate`)~~ (R28) — **corrigido**: `prisma/migrations/` com baseline + 2 migrations reais; script `migrate:deploy` cadastrado em `package.json` (§9 rodadas 5 e 6).
- `.env` foi commitado no histórico do git antes de entrar no `.gitignore` — recomenda-se rotacionar as credenciais reais (banco, JWT, sessão) que circularam; ver §11 item 20 (ação sua, fora do meu acesso).
- ~~Segredo JWT/sessão com fallback hardcoded~~ (`'dev-secret-key'`) — **corrigido**: falha explícita no boot se a env var não estiver definida; sessão passou a usar `SESSION_PRIVATE_KEY` dedicada (§9 rodada 6).
- ~~13 endpoints `DELETE /:id` quebrados~~ — **corrigidos** (R21).
- ~~`cargaHoraria` sem validação~~ — **corrigido** (R24).
- ~~Valor por km/devolutiva hardcoded no código~~ — **corrigido**, agora configurável por env var (R19).

### Médios
- ~~Exclusão física de cadastros auxiliares sem checar uso~~ (R20) — **corrigido**: bloqueia com mensagem clara quando em uso (§9 rodada 6).
- ~~`Baixa.create` falha silenciosamente em duplicidade~~ — **corrigido** (R15, movido para críticos-corrigidos acima).
- ~~`perfilId` não validado contra enum/tabela~~ — **corrigido** (R25, movido para críticos-corrigidos acima).
- ~~`FERIADOS` hardcoded só até 2022, desatualizado~~ — **corrigido**: cálculo dinâmico (R34).
- ~~`$queryRawUnsafe` em `findDuplicateFullNames`~~ — **corrigido**: reescrito com query builder do Prisma.
- ~~Paginação sem teto~~ — **corrigido** (R31).
- ~~Sem `@unique` em `StatusEventos.nome`/`Especialidade.nome`/`Funcao.nome`, sem `@@unique(eventoId)` em `Baixa`~~ — **corrigido** (§9 rodada 6): conferido antes no banco real que não havia duplicidade em nenhum dos 3 primeiros (migration segura); `Baixa.eventoId` idem. `Paciente.carteirinha` **não** ganhou `@unique`: há duplicidade real hoje (inclusive um valor placeholder `"-"` usado 22 vezes) — forçar a constraint quebraria a migration; fica como pendência de limpeza de dado (§11).
- Sem `@@index` em `Calendario`/`Baixa` para as consultas mais frequentes — **corrigido** (§9 rodada 6): `@@index([terapeutaId, start])`, `@@index([pacienteId])`, `@@index([groupId])` em `Calendario`; `@@index([usuarioId])`, `@@index([dataEvento])` em `Baixa`.

### Baixos
- 71 warnings de lint, 35 erros de formatação Prettier — não tratado (mudança mecânica de baixo valor/alto diff, fora do escopo desta rodada).
- Dois mecanismos de sessão coexistindo sem necessidade — não tratado (a sessão passou a usar segredo próprio corretamente, mas remover `express-session`/`passport.session()` de vez, se o frontend não depender de cookie de sessão em lugar nenhum, é uma limpeza separada, não investigada a fundo).
- ~~`console.log` no lugar de logger estruturado~~ — **parcialmente corrigido**: requisições e erros agora passam pelo logger estruturado do Nest (interceptor + filtro globais); `console.log`/`console.warn` pontuais dentro dos services não foram todos substituídos (mudança mecânica de baixo valor, não priorizada).

---

## 5. Regras somente no frontend

Todas as regras que estavam listadas aqui como "só frontend" nas versões anteriores deste relatório foram corrigidas — a última leva (grupo de permissão, reset de senha, exclusão de baixa, snapshot financeiro, exclusão de série recorrente) na rodada 6. Não há nenhuma pendência conhecida nesta categoria no momento; o que resta em aberto (§10/§11) ou já tem alguma validação parcial no backend, ou depende de decisão de negócio ainda não tomada (não é "regra ignorada pelo backend por omissão").

**Corrigidas ao longo da sessão (deixaram de ser só-frontend):** conflito de horário do terapeuta, jornada 8h–20h, antecedência de cancelamento, vínculo especialidade↔paciente/terapeuta/função, campos bloqueados após criação do evento, exclusão/edição de evento passado (incluindo a regra "só Atestado" e a tolerância de 2h), 401 vs 403 diferenciados, grupo de permissão só editável por ADM, reset de senha seguro, exclusão de baixa auditada, snapshot financeiro, exclusão de série bloqueada com sessão realizada — todas validadas no backend, ver §3 (R2, R3, R4, R5, R6, R7, R8, R9, R10, R11, R16, R17, R24, R26, R27, R32, R33).

---

## 6. Endpoints encontrados (amostra)

| Método | Rota | Controller | Permissão | Observação |
|---|---|---|---|---|
| POST | `/login` | auth.controller.ts | `AuthGuard('local')` | OK |
| POST | `/evento` | agenda.controller.ts | `AuthGuard('jwt')` + `AGENDA_CALENDARIO_FILTRO_BOTAO_CADASTRAR` | ✅ Conflito de horário, jornada e vínculo especialidade/terapeuta/função validados (`validateEvento`); snapshot financeiro gravado; tag exigida (rodada 6) |
| PUT | `/evento` | agenda.controller.ts | `AuthGuard('jwt')` + `AGENDA_CALENDARIO_LISTA_EDITAR` | ✅ Campos bloqueados travados, revalida conflito/jornada se a terapeuta muda, status de cancelamento recalculado (48h), evento passado só aceita status Atestado (tolerância 2h); tag exigida (rodada 6) |
| DELETE | `/evento` | agenda.controller.ts | `AuthGuard('jwt')` + `AGENDA_CALENDARIO_LISTA_EXCLUIR` | ✅ Bloqueia evento passado (tolerância 2h) e bloqueia série com qualquer ocorrência já realizada; tag exigida (rodada 6) |
| PUT | `/evento/check`, `/evento/atestado` | agenda.controller.ts | `AuthGuard('jwt')` (sem tag) | Check-in mobile e atestado — deixados sem tag de propósito: mapeamento pra uma tag específica do catálogo não é óbvio pra essas duas ações |
| POST | `/usuarios` | user.controller.ts | `AuthGuard('jwt')` + `CADASTRO_USUARIOS_BOTAO_CADASTRAR` | ✅ `grupoPermissaoId` não é mais aceito aqui (rota própria); senha aleatória + troca obrigatória; perfil Developer só atribuível por Developer (rodada 6) |
| PUT | `/usuarios/:id/grupo-permissao` | user.controller.ts (novo) | `AuthGuard('jwt')` + `CADASTRO_GRUPO_PERMISSOES_LISTA_BOTAO_EDITAR` | ✅ Novo (rodada 6) — único lugar que altera `grupoPermissaoId` de um usuário existente |
| GET | `/usuarios/reset-senha/:id` | user.controller.ts | `AuthGuard('jwt')` + `CADASTRO_USUARIOS_LISTA_BOTAO_RESETAR_SENHA` | ✅ Senha aleatória + `mustChangePassword` (rodada 6) |
| PUT | `/vagas/agendar`, `/vagas/devolutiva` | vaga.controller.ts | `AuthGuard('jwt')` (sem tag) | ✅ Guard reativado (estava comentado/público); segue sem tag — a mesma rota atende avaliação/devolutiva/terapia, sem 1:1 claro no catálogo |
| PUT | `/baixa` | baixa.controller.ts | `AuthGuard('jwt')` | ✅ `usuarioId` agora vem do JWT, não mais do corpo da requisição |
| DELETE | `/baixa/:id` | baixa.controller.ts | `AuthGuard('jwt')` + `AGENDA_BAIXA_DELETE` | ✅ Exige `motivo`, grava snapshot em `BaixaExclusaoLog` antes de apagar (rodada 6) |
| DELETE | `/paciente/:id`, `/especialidade/:id`, `/funcao/:id`, `/localidade/:id`, `/status-eventos/:id`, `/modalidade/:id`, `/periodo/:id`, `/perfil/:id`, `/status/:id`, `/tipo-sessao/:id`, `/convenio/:id`, `/frequencia/:id`, `/sessao/:id` | 13 controllers | `AuthGuard('jwt')` (+ tag em 6 delas, ver R1) | ✅ Bug de `@Param()` corrigido; Especialidade/Função/Localidade/Status-eventos agora bloqueiam exclusão se a entidade estiver em uso (rodada 6) |

---

## 7. Modelo de dados

- `Terapeuta.especialidadeId` (1 especialidade) + `TerapeutaOnFuncao` (N funções, com `comissao`/`tipo` por função) — **modelagem correta**, confirmada pelo negócio.
- ~~Ausência de snapshot financeiro em `Calendario`/`Baixa`~~ — **corrigido** (rodada 6): `Calendario` ganhou `valorSessaoSnapshot`/`comissaoSnapshot`/`tipoComissaoSnapshot`/`valorPorKmSnapshot`/`valorSessaoDevolutivaSnapshot`.
- Datas e valores monetários como `String`, não `DateTime`/`Decimal` — datas continuam `String` (fora de escopo); valores monetários já migrados para `Decimal` desde a rodada 3 (R18), esta linha ficou desatualizada nas versões anteriores do relatório.
- ~~Sem `@unique` em `Paciente.carteirinha`, `StatusEventos.nome`, `Especialidade.nome`, `Funcao.nome`~~ — **parcialmente corrigido** (rodada 6): `StatusEventos.nome`, `Especialidade.nome` e `Funcao.nome` (composto com `especialidadeId`) ganharam `@unique`, confirmado sem duplicidade real antes de migrar. `Paciente.carteirinha` **não** ganhou — há duplicidade real nos dados hoje (inclusive um placeholder `"-"` usado 22 vezes), forçar a constraint quebraria a migration; fica pendente de uma decisão de limpeza de dado (§11).
- ~~`Baixa` sem `@@unique([eventoId])`~~ — **corrigido** (rodada 6), sem duplicidade real encontrada nos dados (NULLs não contam como duplicata no MySQL).
- ~~Sem `@@index` em `Calendario`/`Baixa` para as consultas mais frequentes~~ — **corrigido** (rodada 6): `Calendario(terapeutaId, start)`, `Calendario(pacienteId)`, `Calendario(groupId)`, `Baixa(usuarioId)`, `Baixa(dataEvento)`.
- Campo `ativo` existe em vários cadastros mas os `delete()` de service fazem hard delete em vez de usá-lo — segue assim; R20 (rodada 6) resolveu o risco de FK crua bloqueando a exclusão quando em uso, mas não trocou o modelo de "excluir" por "desativar" quando *não* está em uso.

---

## 8. Testes

Estado original: 31 testes unitários (formatação/filtro), 8 suites. Nenhum cobria: permissões, conflito de horário, transição de fila, cancelamento com/sem antecedência, baixa duplicada, financeiro com snapshot, concorrência, isolamento entre clínicas. Nenhum teste e2e.

**Após as rodadas 1–5: 73 testes, 11 suites.** Distribuição dos 42 novos até ali:

- [agenda.service.spec.ts](src/agenda/agenda.service.spec.ts) (19): `hasScheduleConflict` (sobreposição parcial, horário idêntico, encostado sem sobrepor, evento cancelado ignorado), `validateJornada` (fora de 8h–20h, início ≥ fim, dia sem jornada cadastrada, dentro da jornada), `isEventoPassado`/`assertSomenteStatusAlterado` (passado vs. futuro, dentro/fora da tolerância de 2h, campo bloqueado rejeitado, só status permitido), `assertStatusPermitidoParaEventoPassado` (só Atestado aceito), `resolveStatusCancelamento` (status não-cancelamento intocado, <48h/≥48h).
- [response.spec.ts](src/util/response.spec.ts) (11): status/mensagem reais por `HttpException` (401/403/404/400), por código do Prisma (P2025/P2002/P2003), por `Error` genérico, por string customizada, fallback 500 sem vazar stack.
- [format-date.spec.ts](src/util/format-date.spec.ts) (3): feriados fixos e móveis de um ano, offset do Carnaval em relação à Páscoa em anos distintos.
- [baixa.service.spec.ts](src/baixa/baixa.service.spec.ts) (+2): duplicidade sinalizada sem criar de novo, criação normal quando não há duplicidade.
- [permissions.guard.spec.ts](src/auth/permissions.guard.spec.ts) (7): rota sem `@RequirePermission` libera direto, sem usuário autenticado bloqueia, perfil Developer sempre libera, usuário com a tag libera, usuário sem a tag rejeita com 403, usuário sem grupo é tratado como sem permissão nenhuma, usuário inexistente bloqueia.

**Após a rodada 6: 119 testes, 18 suites.** Distribuição dos 46 novos:

- [user.service.spec.ts](src/user/user.service.spec.ts) (9, arquivo novo): senha nunca fixa + `mustChangePassword`, bloqueio de atribuir perfil Developer sem já ser Developer, `grupoPermissaoId` ignorado no update, `updateGrupoPermissao` (rejeita grupo inexistente / aceita `null` / grava quando existe), reset por terceiro gera senha aleatória, troca pelo próprio usuário limpa `mustChangePassword`.
- [password.spec.ts](src/util/password.spec.ts) (3, arquivo novo): tamanho da senha, sem caracteres ambíguos, aleatoriedade real (nunca `'12345678'`).
- [permissions.guard.spec.ts](src/auth/permissions.guard.spec.ts) (+2): `mustChangePassword` pendente bloqueia mesmo com a tag, inclusive para Developer.
- [baixa.service.spec.ts](src/baixa/baixa.service.spec.ts) (+3): exclusão sem motivo rejeitada, baixa inexistente rejeitada, snapshot + motivo + usuário gravados em `BaixaExclusaoLog` antes de excluir.
- [agenda.service.spec.ts](src/agenda/agenda.service.spec.ts) (+6): `computeFinanceiroSnapshot` (valores encontrados / `null` quando paciente sem vaga ou terapeuta sem a função), exclusão de série bloqueada com sessão já realizada em outra ocorrência da mesma série / permitida quando toda a série é futura.
- [vaga.service.spec.ts](src/vaga/vaga.service.spec.ts) (5, arquivo novo): `update()` roda dentro de um único `$transaction`, usa o client `tx` (não o client base) nas escritas, repassa `tx` para `PacienteService`, aguarda de verdade a transição final (bug de `Promise` sem `await` corrigido), grava o histórico do paciente na mesma transação.
- [financeiro.service.spec.ts](src/financeiro/financeiro.service.spec.ts) (+3): `paciente()`/`terapeuta()` preferem o snapshot ao valor atual do cadastro quando presente, incluindo tarifa de km e de devolutiva.
- [assert-not-in-use.spec.ts](src/util/assert-not-in-use.spec.ts) (3, arquivo novo): não lança quando nada está em uso, lança a mensagem informada quando alguma checagem encontra uso, passa o `where` certo por model.
- [especialidade.service.spec.ts](src/especialidade/especialidade.service.spec.ts) (3, arquivo novo) e [status-evento.service.spec.ts](src/status-evento/status-evento.service.spec.ts) (3, arquivo novo): `delete()` bloqueado quando em uso, executado normalmente quando não está.
- [main.spec.ts](src/main.spec.ts) (+3) e [jwt.strategy.spec.ts](src/auth/jwt.strategy.spec.ts) (4, arquivo novo): segredo de sessão prioriza `SESSION_PRIVATE_KEY`, cai para `JWT_PRIVATE_KEY`, nunca cai num fallback hardcoded (lança erro); `JwtStrategy` idem no construtor; extração de token de headers alternativos/query string.

Ainda faltam: testes de `createCalendario`/`updateCalendario` ponta a ponta (com Prisma mockado ou banco de teste), concorrência real (duas requisições simultâneas — hoje só testado que a lógica roda dentro de `$transaction`, não o comportamento do banco sob concorrência de verdade), isolamento entre clínicas (não aplicável, `Clinica` não existe) e e2e — não cobertos.

---

## 9. Ajustes já aplicados nesta sessão

Escopo desta primeira leva: **apenas correções mecânicas e não-ambíguas**, sem decisão de negócio pendente e sem mudança de contrato de API além de corrigir o que já estava quebrado. Cada um foi validado com `tsc --noEmit`, `nest build` e `npx jest` (31/31 continuam passando).

| # | Arquivo(s) | O que estava errado | O que foi feito |
|---|---|---|---|
| 1 | 13 controllers (`paciente`, `especialidade`, `funcao`, `localidade`, `status-evento`, `modalidade`, `periodo`, `perfil`, `status`, `tipo-sessao`, `convenio`, `frequencia`, `sessao`) | `@Param() id: number` (sem chave) fazia o Nest injetar o objeto `req.params` inteiro em vez do valor de `id`; todo `DELETE /:id` sempre falhava (`Number({...})` = `NaN`) | Trocado para `@Param('id') id: string` e conversão para número feita no service (que já usava `Number(id)`/`toNumberId(id)`) |
| 2 | [src/paciente/paciente.service.ts](src/paciente/paciente.service.ts) | `delete(id)` executava `prisma.localidade.delete(...)` — apagava um registro de **Localidade**, não o paciente | Reescrito para inativar o paciente (`prisma.paciente.update({ where: { id }, data: { disabled: true } })`), preservando histórico clínico/financeiro em vez de excluir fisicamente |
| 3 | [src/vaga/vaga.controller.ts](src/vaga/vaga.controller.ts) | `@UseGuards(AuthGuard('jwt'))` estava **comentado** — toda a fila de pacientes ficava acessível sem login | Guard reativado |
| 4 | [src/vaga/vaga.service.ts](src/vaga/vaga.service.ts) | `switch` sem `break` entre `case STATUS_PACIENT_COD.queue_avaliation` e `case STATUS_PACIENT_COD.avaliation` em `update()` — os dois blocos rodavam juntos por engano | Adicionado `break` ao final do bloco `queue_avaliation` |
| 5 | [src/agenda/agenda.service.ts](src/agenda/agenda.service.ts) | `this.baixaService.create(...)` era chamado **sem `await`** em 4 pontos (`updateCalendario`, `updateEventoUnicoGrupo`, `updateEventoRecorrentes`, `updateEventoRecorrentesAllChange`) — erro viraria unhandled rejection e a resposta ao cliente não esperava a baixa ser gravada | Adicionado `await` em todas as chamadas |
| 6 | [src/util/pagination.ts](src/util/pagination.ts) + pontos de uso | `pageSize` vindo do cliente sem teto — permitia dump de tabela inteira | Adicionado teto máximo de página (defensivo, não quebra contrato existente) |

### Rodada 2 — identidade, agenda (conflito/jornada/vínculos), recorrência, cancelamento e evento passado

Antes de começar, 4 decisões de negócio que estavam pendentes (§11 da v1) foram fechadas com você:

* **Autorização por tag**: adiada. Sem catálogo de permissões existente, implementar guard de tag agora arriscava travar acesso de usuários legítimos. Nesta rodada só a **identidade** (quem está fazendo a chamada) foi corrigida — a checagem "essa pessoa pode fazer isso" continua pendente para quando o catálogo de tags for definido.
* **Antecedência de cancelamento**: 48 horas corridas até o início do evento (não 24h, não D-1).
* **Recorrência sem `dataFim`**: é o comportamento esperado (paciente ainda em atendimento, sem previsão de alta) — não é um bug. Nenhuma trava foi adicionada aqui; a arquitetura já existente (1 linha por série + `groupId` + `exdate` + `isChildren` para exceções) foi mantida e um bug real dentro dela foi corrigido (ver item 8 abaixo).
* **Campos bloqueados enviados diferentes do salvo**: ignorados silenciosamente (mantém o valor original), sem rejeitar a requisição inteira.

Todos os itens abaixo foram validados com `tsc --noEmit`, `nest build` e `npx jest` (50/50 passando, 19 testes novos cobrindo especificamente esta rodada em [agenda.service.spec.ts](src/agenda/agenda.service.spec.ts)).

| # | Arquivo(s) | O que estava errado | O que foi feito |
|---|---|---|---|
| 7 | [src/agenda/agenda.controller.ts](src/agenda/agenda.controller.ts), [src/user/user.controller.ts](src/user/user.controller.ts), [src/sessao/sessao.controller.ts](src/sessao/sessao.controller.ts) | Identidade de quem cria/edita/exclui vinha de `req.headers.login`, um header livre que o cliente controla | Trocado por `req.user?.username`, que o Passport preenche a partir do payload **assinado** do JWT (`sub`/`username`), verificado na estratégia `jwt.strategy.ts` |
| 8 | [src/baixa/baixa.controller.ts](src/baixa/baixa.controller.ts), [src/baixa/baixa.service.ts](src/baixa/baixa.service.ts), [src/baixa/baixa.module.ts](src/baixa/baixa.module.ts) | `usuarioId` de quem deu baixa vinha direto do corpo da requisição (`body.usuarioId`) — o cliente podia atribuir a baixa a qualquer usuário | `BaixaService.update` agora resolve o usuário a partir do login autenticado (via `UserService`, importado no módulo) |
| 9 | [src/agenda/agenda.service.ts](src/agenda/agenda.service.ts) — `formatEvent` | `changeAll` (via `updateCalendario`) fazia `prisma.calendario.updateMany({ where: { groupId } })` quando já existiam múltiplas linhas no grupo — sobrescrevia `dataInicio`/ocorrências passadas/exceções já materializadas (`isChildren`) do mesmo grupo | Removido esse atalho; `changeAll` agora sempre passa por `updateEventoRecorrentes` → `updateEventoRecorrentesAllChange`, que faz o split correto (série antiga ganha `dataFim` de corte, série nova nasce da data atual) — o mesmo padrão que você descreveu já usar |
| 10 | [src/agenda/agenda.service.ts](src/agenda/agenda.service.ts) — `formatEvent(event, original)` | Nenhum campo era travado após a criação do evento | `formatEvent` agora aceita o registro original e sempre mantém dele `dataInicio`, `start`, `end`, `modalidadeId`, `frequenciaId`, `intervaloId`, `diasFrequencia`, ignorando silenciosamente valor diferente enviado pelo cliente. Aplicado nos 4 pontos de update (edição de ocorrência única, "só esta", "esta e as próximas", evento único) |
| 11 | [src/agenda/agenda.service.ts](src/agenda/agenda.service.ts) — `validateAgendamentoVinculos` | `createCalendario` não validava se a especialidade pertence ao paciente, se a terapeuta possui a especialidade/função, ou se paciente/terapeuta/função/status/local estão ativos | Novo validador chamado na criação (evento default e devolutiva) e em toda edição que muda paciente/especialidade/terapeuta/função/local/status; rejeita IDs incompatíveis mesmo enviados manualmente |
| 12 | [src/agenda/agenda.service.ts](src/agenda/agenda.service.ts) — `validateJornada` | Jornada da terapeuta (`cargaHoraria`, 8h–20h) só era usada para montar a grade visual, nunca para validar criação/edição | Novo validador: rejeita horário fora de 08:00–20:00, `start >= end`, e qualquer dia/horário fora do `cargaHoraria` cadastrado da terapeuta. Roda na criação e sempre que a terapeuta muda numa edição |
| 13 | [src/agenda/agenda.service.ts](src/agenda/agenda.service.ts) — `hasScheduleConflict` | Nenhuma checagem de conflito de horário em lugar nenhum — dupla marcação era sempre aceita | Novo validador: materializa as datas da série nova e de cada série existente da mesma terapeuta (respeitando frequência/intervalo/exdate, com teto de 365 dias para série sem fim) e rejeita sobreposição de horário; ignora eventos cancelados |
| 14 | [src/agenda/agenda.service.ts](src/agenda/agenda.service.ts) — `resolveStatusCancelamento` | Cliente escolhia livremente entre "Cancelado com Antecedência" (não cobra) e "Cancelado sem Antecedência" (cobra) | Backend recalcula com base em 48h corridas até o início da ocorrência sendo alterada, sempre que o status enviado for um desses dois — silenciosamente substitui pelo correto. A criação de baixa decorrente agora usa o status corrigido, não o enviado |
| 15 | [src/agenda/agenda.service.ts](src/agenda/agenda.service.ts) — `isEventoPassado` / `assertSomenteStatusAlterado` | Evento com horário final já passado podia ser editado (qualquer campo) ou excluído livremente | `delete()` agora rejeita evento já ocorrido; toda edição de evento passado só aceita mudança de `statusEventosId` — qualquer outro campo diferente do original (paciente/especialidade/terapeuta/função/local/observação) é rejeitado com erro |
| 16 | [src/agenda/agenda.service.ts](src/agenda/agenda.service.ts) — `delete()` | Erros dentro de `delete()` (incluindo o novo bloqueio de evento passado) eram só logados no console; o controller respondia sucesso mesmo sem excluir nada | `catch` agora relança o erro, que chega ao controller como falha real |
| 17 | [src/agenda/agenda.service.ts](src/agenda/agenda.service.ts) — `assertStatusPermitidoParaEventoPassado` | "Alteração de status autorizada" em evento passado não tinha lista definida | Fechado com você: **apenas Atestado**. Qualquer outro status em evento passado é rejeitado, mesmo que `assertSomenteStatusAlterado` já garanta que só o status mudou |
| 18 | [src/agenda/agenda.service.ts](src/agenda/agenda.service.ts) — `isEventoPassado` / `TOLERANCIA_EVENTO_PASSADO_HORAS` | A regra "só Atestado" do item 17 quebraria o check-in mobile (`PUT /evento/check`, marca Atendido), que normalmente roda durante/logo após a sessão | Fechado com você: tolerância de **2 horas após o término** — dentro dessa janela o evento não é considerado "passado" (qualquer status vale); só depois das 2h a trava de "somente Atestado" passa a valer |

**Fora do escopo desta rodada (decisão de negócio pendente ou exige migration de banco — ver §11):** autorização por tag, whitelist de `grupoPermissaoId`/`perfilId` (escalada de privilégio), permissão para reset de senha, snapshot financeiro (`Calendario`/`Baixa`), soft delete/auditoria de baixa e cadastros em uso, `prisma.$transaction` real nas transições de fila, unicidade de carteirinha/nomes, índices de banco, migrations formais (o seed foi criado nesta sessão, ver rodada 3).

### Rodada 3 — CORS, migration financeira, 404, tratamento de erro, feriados, seed e monitoramento

Validado com `tsc --noEmit`, `nest build`, `npx jest` (73/73 passando, ver rodada 4 abaixo para os 7 mais recentes).

| # | Arquivo(s) | O que estava errado | O que foi feito |
|---|---|---|---|
| 19 | [src/main.ts](src/main.ts) | `app.enableCors()` aberto + lista restrita com `/` no final da origem de produção (nunca batia com o header `Origin` real) + middleware `cors({origin:'*'})` duplicado em `app.module.ts` | Consolidado em uma única chamada `enableCors`, origem de produção corrigida (sem `/`), middleware duplicado removido |
| 20 | `VagaOnEspecialidade.valor`/`.km`, `TerapeutaOnFuncao.comissao`, `Calendario.km` (schema + banco remoto) | Campos `String`, com valores gravados como `" 200,00"` (espaço não-quebrável + vírgula) — um `ALTER TABLE` cru teria truncado esses valores | Dados normalizados linha a linha em JS (`.trim()` cobre NBSP, que o `TRIM()` do MySQL não remove) antes de qualquer mudança de schema; campos migrados para `Decimal(10,2)` no `prisma/schema.prisma` **e aplicados no banco real** (`prisma db push --accept-data-loss`, confirmado via `information_schema.COLUMNS`: os 4 campos já são `decimal`) |
| 21 | [src/paciente/paciente.service.ts](src/paciente/paciente.service.ts), [src/user/user.service.ts](src/user/user.service.ts), [src/financeiro/financeiro.service.ts](src/financeiro/financeiro.service.ts), [src/agenda/agenda.service.ts](src/agenda/agenda.service.ts) | Leitura/escrita desses 4 campos via `.split('R$')[1]`, `.replace(',', '.')` e `parseFloat` manual espalhados pelo código — quebrariam com `Decimal` do Prisma (não tem `.replace`) | Centralizado em dois helpers novos em [src/util/normalizers.ts](src/util/normalizers.ts): `normalizeCurrencyValue` (escrita, aceita "R$ 200,00"/"200,00"/número) e `readDecimal` (leitura seguro de `Decimal` do Prisma) |
| 22 | [src/financeiro/financeiro.service.ts](src/financeiro/financeiro.service.ts) | `* 0.9` (valor/km) e `= 50` (valor de sessão de devolutiva) hardcoded no meio do cálculo | Externalizado para `FINANCEIRO_VALOR_POR_KM`/`FINANCEIRO_VALOR_SESSAO_DEVOLUTIVA` (env var, com default igual ao valor antigo — nada muda até alguém configurar) |
| 23 | [src/main.ts](src/main.ts) | Nenhum controller tinha prefixo `api/`, sem `setGlobalPrefix` — toda rota só existia em `/algo`, nunca `/api/algo`. Frontend chama `/api/especialidade` → 404 | `app.setGlobalPrefix('api')` |
| 24 | [src/especialidade/especialidade.controller.ts](src/especialidade/especialidade.controller.ts), [src/especialidade/especialidade.service.ts](src/especialidade/especialidade.service.ts) | Além do prefixo, `EspecialidadeController` nunca teve endpoint `GET` paginado (só tinha `dropdown`/`:search`/`create`/`update`/`delete`) — era a segunda causa do 404 do print | Adicionado `getAll(page, pageSize)`, no mesmo padrão dos módulos irmãos (função, localidade, etc.) |
| 25 | [src/util/response.ts](src/util/response.ts) | `responseError` sempre retornava `401 { message: 'Erro na conexão!' }`, e a maioria dos ~150 `catch` nos controllers nem repassava o `error` capturado | Reescrito: resolve status/mensagem reais a partir do tipo do erro (`HttpException` → status real; `Prisma.PrismaClientKnownRequestError` P2025/P2002/P2003 → 404/409/409 com mensagem amigável; `Error` genérico → 400 com a mensagem; string customizada → 400; desconhecido → 500 genérico sem vazar stack). Todos os ~150 `catch (error) { responseError(response); }` dos controllers agora passam `error` de verdade. 11 testes novos em [response.spec.ts](src/util/response.spec.ts) |
| 26 | [src/baixa/baixa.service.ts](src/baixa/baixa.service.ts) | `create()` retornava `undefined` em silêncio quando já existia baixa para o evento, e um `catch` genérico engolia qualquer erro real | Duplicidade agora loga (`console.warn`) e retorna `{ created: false, duplicate: true, baixa }` em vez de `undefined`; catch-all removido (erro real propaga). 2 testes novos |
| 27 | [src/user/user.service.ts](src/user/user.service.ts) | `perfilId` era aceito sem checar se existe na tabela `Perfil` | Novo `validatePerfilId()`, chamado em `create()` e `update()`; rejeita id inexistente/ausente antes de gravar |
| 28 | [src/util/format-date.ts](src/util/format-date.ts) | `FERIADOS` era uma lista fixa só até 2022 (evento/disponibilidade calculado a partir de 2023 não considerava feriado nenhum); além disso `holidayFormat: 'YYYY-MM-DD'` estava configurado mas a lista antiga usava `DD-MM-YYYY` — nunca bateu | Feriados nacionais agora calculados dinamicamente (fixos + móveis via Páscoa, algoritmo de Meeus/Jones/Butcher) para uma janela de anos ao redor de hoje, no formato correto. Não cobre feriado municipal/estadual. 3 testes novos em [format-date.spec.ts](src/util/format-date.spec.ts) |
| 29 | [src/paciente/paciente.service.ts](src/paciente/paciente.service.ts) | `findDuplicateFullNames()` usava `$queryRawUnsafe` com SQL escrito à mão (sem input do usuário hoje, mas padrão arriscado de manter) | Reescrito com o query builder do Prisma (`findMany` + agrupamento em JS) |
| 30 | [prisma/seed.ts](prisma/seed.ts) (novo) | Sem seed nenhum — R28 do diagnóstico original | Seed gerado a partir dos dados **reais atuais** de 18 tabelas de cadastro/referência (convênio, especialidade, função, localidade, status de evento, frequência, modalidade, intervalo, período, status, tipo de sessão, status de paciente, perfil, permissão, grupo de permissão, programa, atividades VB-MAPP/Portage). **Decisão tomada por mim, não pedida explicitamente**: excluí `Usuario`/`Terapeuta`/`TerapeutaOnFuncao`/`Paciente`/`PacienteHistorico` do seed além de agendamento/filas — são dados de pessoas reais (nome, telefone, hash de senha) e versionar isso no git exporia PII permanentemente no histórico. Idempotente (`upsert`), configurado em `package.json` (`npm run seed` / `npx prisma db seed`). Não rodei contra o banco remoto (upsert de um snapshot antigo poderia sobrescrever uma edição concorrente feita depois do dump) |
| 31 | [src/util/logging.interceptor.ts](src/util/logging.interceptor.ts), [src/util/all-exceptions.filter.ts](src/util/all-exceptions.filter.ts) (novos) | Nenhum log estruturado de requisição/erro — só `console.log` esparsos e inconsistentes pelos services | Interceptor global loga toda requisição (método, rota, usuário, status, duração); filtro global captura erro que escapa do try/catch de controller (guard, pipe) usando a mesma lógica de status/mensagem do item 25. Smoke-testado manualmente (sem token → 401 logado; rota inexistente → 404 logado; login inválido → 401 com mensagem real logado) |
| 32 | [tsconfig.build.json](tsconfig.build.json) | **Bug que eu mesmo introduzi** ao criar `prisma/seed.ts`: sem esse arquivo excluído do build, o TypeScript recalculava o `rootDir` para a raiz do projeto (em vez de `src/`), e `nest build` passou a gerar `dist/src/main.js` em vez de `dist/main.js` — quebrando `npm run start:prod` (`node dist/main`) e o `Procfile` de produção | Adicionado `"prisma"` ao `exclude` de `tsconfig.build.json`; rebuild limpo confirmou `dist/main.js` de volta ao lugar certo antes de considerar a rodada concluída |

### Rodada 4 — autorização por tag nas rotas de maior risco

O seed (item 30) trouxe o catálogo real de `Permissao`/`GrupoPermissao`/`GrupoPermissaoOnPermissao` (154 tags, 5 grupos). Inspecionei os dados antes de codar: as tags são de granularidade de **UI** (menu/botão/campo — ex.: `CADASTRO_USUARIOS_LISTA_BOTAO_RESETAR_SENHA`, `AGENDA_CALENDARIO_EVENTO_EDITAR_TERAPEUTA`), não de rota de API 1:1. Por isso a autorização por tag só foi aplicada onde o mapeamento tag → ação é inequívoco — as rotas mais perigosas identificadas desde a v1 do relatório (R1/R3/R4).

Validado com `tsc --noEmit`, `nest build`, `npx jest` (73/73 passando, 7 testes novos em [permissions.guard.spec.ts](src/auth/permissions.guard.spec.ts)).

| # | Arquivo(s) | O que estava errado | O que foi feito |
|---|---|---|---|
| 33 | [src/auth/permissions.guard.ts](src/auth/permissions.guard.ts), [src/auth/require-permission.decorator.ts](src/auth/require-permission.decorator.ts) (novos) | Nenhum guard checava tag de permissão em rota nenhuma | `PermissionsGuard` + `@RequirePermission('cod')`: busca o grupo do usuário autenticado (pelo login do JWT) e confere se tem alguma das tags exigidas; perfil "Developer" sempre passa (mesmo bypass já usado no login); rota sem `@RequirePermission` não é afetada |
| 34 | [src/user/user.controller.ts](src/user/user.controller.ts) | `POST/PUT /usuarios` e `GET /usuarios/reset-senha/:id` abertos a qualquer autenticado | Agora exigem `CADASTRO_USUARIOS_BOTAO_CADASTRAR`, `CADASTRO_USUARIOS_LISTA_BOTAO_EDITAR` e `CADASTRO_USUARIOS_LISTA_BOTAO_RESETAR_SENHA` respectivamente. As rotas de troca da **própria** senha (`PUT /usuarios/reset-senha`, `PUT /usuarios/reset-senha/:login`) continuam sem tag — não são a mesma ação que resetar a senha de terceiro |
| 35 | [src/grupoPermissao/grupoPermissao.controller.ts](src/grupoPermissao/grupoPermissao.controller.ts) | `POST/PUT /grupo-permissoes` abertos a qualquer autenticado | Agora exigem `CADASTRO_GRUPO_PERMISSOES_BOTAO_CADASTRAR`/`_LISTA_BOTAO_EDITAR` |
| 36 | [src/baixa/baixa.controller.ts](src/baixa/baixa.controller.ts) | `PUT /baixa` e `DELETE /baixa/:id` abertos a qualquer autenticado | Agora exigem `AGENDA_BAIXA_UPDATE`/`AGENDA_BAIXA_DELETE` |
| 37 | [src/paciente/paciente.controller.ts](src/paciente/paciente.controller.ts) | `DELETE /paciente/:id` e `PUT /paciente/desabilitar` abertos a qualquer autenticado | Ambos agora exigem `CADASTRO_PACIENTES_LISTA_BOTAO_EXCLUIR` |
| 38 | [src/user/user.service.ts](src/user/user.service.ts) — `findUserAuth`/`getUser` | **Bug real encontrado no processo, fora do que foi pedido**: `user.permissoes = user.grupo.permissoes` quebrava (`TypeError`) para qualquer usuário com `grupoPermissaoId` nulo. Conferi no banco real: **muitos usuários ativos hoje têm `grupo: null`** (`tati.granado`, `larissa.lima`, `alda.carrara`, `caroline.viana` e outros) | `user.permissoes = user.grupo?.permissoes \|\| []` nos dois pontos — parou de estourar `TypeError`. **Ressalva que essa linha subestimava**: mesmo sem o crash, `AuthService.login()` ainda rejeita o login com `'Não há permissão para esse módulo'` pra quem não tem grupo (checagem de tag `DEVICE_WEB`/`DEVICE_MOBILE`, sem relação com senha) — o usuário continuava sem conseguir logar, só com uma mensagem diferente. Corrigido de verdade depois da rodada 6 (ver nota abaixo) |

> **Nota pós-rodada 6:** ao testar o reset de senha (R3) com `alda.carrara`, você bateu exatamente nesse problema — login negado com `'Não há permissão para esse módulo'`, sem relação com a senha resetada. Investigando, achei **20 contas ativas sem grupo** (19 `Terapeuta` + `tati.granado`, `Secretária`) — nenhuma delas conseguia logar. Com sua confirmação: as 19 contas de Terapeuta foram movidas pro grupo `TERAPEUTA` (id 6, já tinha as tags `DEVICE_WEB`+`DEVICE_MOBILE` — só nunca tinha sido atribuído a ninguém); `tati.granado` foi movida pro grupo `RECEPCAO` a seu pedido, mas **esse grupo não tem tag de dispositivo nenhuma hoje** — ela continua sem conseguir logar até `RECEPCAO` ganhar `DEVICE_WEB` (ou ela ser movida pra outro grupo), decisão que fica pendente (ver §11). Nenhum outro usuário ativo ficou sem grupo depois dessa correção.

**Observação sobre os dados reais (não é bug de código, é configuração)**: os grupos `RECEPCAO` e `RECEPCAO BASICO` têm hoje exatamente as mesmas tags perigosas que `ADM` — incluindo resetar senha de terceiro e criar/editar grupo de permissão. O `PermissionsGuard` preserva esse comportamento (é o que está configurado no banco); se não for intencional, é uma mudança de dados em `GrupoPermissaoOnPermissao`, não uma correção de código, e não fiz essa mudança sem você confirmar.

**Continua fora do escopo**: as demais rotas de agenda e dos outros cadastros (modalidade, status-eventos, frequência, função, localidade, programa) têm tags equivalentes disponíveis no catálogo e poderiam ser protegidas com o mesmo padrão — não fiz por conta própria para não arriscar travar fluxo de trabalho de grupos sem revisão sua tag a tag. `permissao`/`perfil` (CRUD dos próprios cadastros de permissão/perfil) não têm tag correspondente no catálogo atual.

### Rodada 5 — migrations formais + preparação (não conectada) para snapshot financeiro e checagem de permissão reutilizável

Esta rodada só tem **um item efetivamente concluído** (migrations). Os outros dois arquivos novos são preparação para trabalho futuro — documentados aqui para não ficarem invisíveis no diff, mas **não mudam nenhum status da matriz** além do já listado.

| # | Arquivo(s) | Situação |
|---|---|---|
| 39 | `prisma/migrations/` (novo) | ✅ **Concluído** — projeto passou de `db push` para `prisma migrate`. Migration `20260805110114_init` é um baseline (marcada como aplicada via `prisma migrate resolve --applied`, não executa nada); `20260805123719_baixa_exclusao_log` é a primeira migration real, cria a tabela `BaixaExclusaoLog`. Ver [prisma/migrations/README.md](prisma/migrations/README.md) para o fluxo (sem shadow database, porque o usuário do banco não tem `CREATE DATABASE`). **Resíduo**: o README referencia `npm run migrate:deploy`, script que ainda não existe em `package.json` (R28) |
| 40 | [prisma/migrations/20260805123719_baixa_exclusao_log/migration.sql](prisma/migrations/20260805123719_baixa_exclusao_log/migration.sql) | ⚠️ **Não conectado nesta rodada** — conectado na rodada 6 logo abaixo (item 44) |
| 41 | [src/util/financeiro-config.ts](src/util/financeiro-config.ts) (novo) | ⚠️ **Não conectado nesta rodada** — conectado na rodada 6 logo abaixo (item 45) |
| 42 | [src/auth/permission.utils.ts](src/auth/permission.utils.ts) (novo) | ⚠️ **Continua não conectado** — `userHasPermission()` replica a lógica do `PermissionsGuard` como função reutilizável, para regras que dependem do estado do recurso (ex.: "só o criador exclui, a menos que..."), que não dá pra expressar só com `@RequirePermission` de rota. Nenhum controller/service chegou a precisar dela até a rodada 6 (R11, o candidato mais óbvio, resolveu com uma regra mais simples: bloquear tudo se houver sessão passada, sem depender de quem é o dono) — segue como utilitário disponível pra quando aparecer o caso de uso |

---

### Rodada 6 — reset de senha seguro, fim do mass assignment, transação real em filas/baixa, snapshot financeiro, exclusão de baixa auditada, exclusão de série bloqueada, autorização por tag estendida, exclusão de cadastro em uso, segredos sem fallback

Rodada maior, disparada por "faça as correções 100% de todos os itens, só pare se eu precisar decidir algo negocial". Antes de começar, 4 decisões de negócio foram fechadas com você (ver §11 "Resolvidas nesta sessão" para o texto exato de cada uma): entrega da nova senha no reset, regra de quem pode atribuir grupo/perfil sensível, granularidade de exclusão de série recorrente, e se o snapshot financeiro deveria ser populado retroativamente. As demais mudanças abaixo não exigiram decisão nova — ou já tinham decisão tomada em rodadas anteriores (ex.: R1 seguiu o mesmo critério "só tag onde o mapeamento é inequívoco" da rodada 4), ou são correções técnicas sem ambiguidade de negócio (transação real, `@@unique`/`@@index` seguros, segredo sem fallback).

Validado com `tsc --noEmit`, `nest build`, `npx jest` (119/119 passando, 46 novos) e um boot real da aplicação (`node dist/main.js`) pra confirmar que nenhum guard novo quebra a inicialização por dependência não resolvida.

| # | Arquivo(s) | O que estava errado | O que foi feito |
|---|---|---|---|
| 43 | `prisma/schema.prisma` + migration `20260805165751_rodada6_seguranca_financeiro` | Faltavam `Usuario.mustChangePassword`, os 5 campos de snapshot financeiro em `Calendario` (4 já existiam soltos no banco real, criados numa sessão anterior sem passar por `schema.prisma`/migration — só `valorSessaoDevolutivaSnapshot` era genuinamente novo), o modelo `BaixaExclusaoLog` (tabela já existia, sem modelo), e os `@unique`/`@@index` abaixo | Migration gerada pelo fluxo manual do projeto (`prisma migrate diff` → `db execute` → `migrate resolve --applied`, documentado em [prisma/migrations/README.md](prisma/migrations/README.md)) — só `ADD COLUMN`/`CREATE INDEX`, nada destrutivo. Antes de aplicar `@unique` em `StatusEventos.nome`/`Especialidade.nome`/`Funcao.nome`/`Baixa.eventoId`, conferi no banco real que não havia duplicidade (senão a migration teria falhado) |
| 44 | [src/baixa/baixa.service.ts](src/baixa/baixa.service.ts), [src/baixa/baixa.controller.ts](src/baixa/baixa.controller.ts) — `delete()` (R16) | Exclusão física, sem motivo, sem quem excluiu, erro só no `console.log` (nunca chegava ao controller) | Agora exige `motivo` (rejeita se vazio), resolve o usuário autenticado, grava snapshot completo da baixa + motivo + usuário em `BaixaExclusaoLog` numa transação, só então apaga fisicamente a `Baixa`. Rota aceita `motivo` no corpo ou na query string |
| 45 | [src/agenda/agenda.service.ts](src/agenda/agenda.service.ts) — `computeFinanceiroSnapshot` (novo), [src/financeiro/financeiro.service.ts](src/financeiro/financeiro.service.ts) (R17) | `FinanceiroService` sempre recalculava do valor *atual* do cadastro (`VagaOnEspecialidade.valor`, `TerapeutaOnFuncao.comissao`) — reajustar um valor hoje reescrevia relatórios de período fechado | Novo método grava `valorSessaoSnapshot`/`comissaoSnapshot`/`tipoComissaoSnapshot`/`valorPorKmSnapshot`/`valorSessaoDevolutivaSnapshot` em `Calendario` sempre que paciente/especialidade/terapeuta/função são definidos (criação ou edição que muda algum desses — não dispara por mudança só de status/local, que não afeta o valor); `financeiro-config.ts` (órfão desde a rodada 5) finalmente importado por ambos os arquivos, eliminando as constantes duplicadas. [prisma/backfill-financeiro-snapshot.ts](prisma/backfill-financeiro-snapshot.ts) populou os eventos que já existiam com o melhor valor disponível hoje (decisão fechada: congelar e popular retroativamente) |
| 46 | [src/vaga/vaga.service.ts](src/vaga/vaga.service.ts), [src/paciente/paciente.service.ts](src/paciente/paciente.service.ts) — `setStatusPaciente`/`setTipoSessaoTerapia` (R13), [src/agenda/agenda.service.ts](src/agenda/agenda.service.ts) — 4 pontos de update+baixa (R12) | `VagaService.update()` usava `Promise.all` solto entre `VagaOnEspecialidade`, `Vaga`, `Paciente` e `PacienteHistorico` — concorrência, não atomicidade; falha parcial deixava paciente inconsistente entre filas. Update do evento + criação da baixa decorrente também sem transação | `VagaService.update()` inteiro passou a rodar dentro de `prisma.$transaction(async (tx) => {...})`, com `tx` propagado por `PacienteService`/`VagaService` (parâmetro opcional, cai pro client normal quando chamado fora de transação). De brinde: dois pontos que retornavam uma `Promise` sem `await` (bug real — a escrita podia terminar fora da transação) foram corrigidos. Os 4 pontos de `AgendaService` que fazem `calendario.update/create` + `baixaService.create` condicional também passaram a rodar numa transação local cada um |
| 47 | [src/agenda/agenda.service.ts](src/agenda/agenda.service.ts) — `delete()` (R11) | Excluir um evento recorrente sempre removia a série inteira, mesmo que parte dela já tivesse ocorrido (sessão já realizada) | Antes de excluir, busca todas as ocorrências da série (`groupId`, incluindo splits `isChildren`) e bloqueia se qualquer uma já passou (`isEventoPassado`). Decisão fechada com você: sem granularidade "só esta"/"esta e as próximas" — se tiver algo passado, a série inteira fica protegida |
| 48 | [src/user/user.service.ts](src/user/user.service.ts), [src/user/user.controller.ts](src/user/user.controller.ts) (R3, R4) | Reset de senha e criação de usuário sempre gravavam `'12345678'`; `POST/PUT /usuarios` aceitavam `grupoPermissaoId`/`perfilId` livremente, sem checagem de quem pode promover pra quê | [src/util/password.ts](src/util/password.ts) (novo) gera senha aleatória sem caracteres ambíguos; `create`/`updatePassword` passaram a usá-la e marcar `Usuario.mustChangePassword = true`, devolvendo a senha em texto plano só na resposta (pra quem tem a permissão repassar manualmente); `updatePasswordLogin` limpa o flag. `grupoPermissaoId` saiu de `create`/`update` — vira `PUT /usuarios/:id/grupo-permissao` (novo endpoint, guardado pela tag de administrar grupo). Atribuir perfil Developer (bypass total) exige que quem chama já seja Developer |
| 49 | [src/auth/permissions.guard.ts](src/auth/permissions.guard.ts), [src/auth/permission.utils.ts](src/auth/permission.utils.ts) | `mustChangePassword` (item 48) precisava de enforcement em algum lugar além do próprio login | `PermissionsGuard` (e o helper `userHasPermission`, por simetria) agora bloqueiam qualquer rota com `@RequirePermission` quando `mustChangePassword` está pendente — mesmo pra perfil Developer. Rotas de troca da própria senha não têm `@RequirePermission`, então nunca ficam presas nesse bloqueio |
| 50 | [src/agenda/agenda.controller.ts](src/agenda/agenda.controller.ts), [src/paciente/paciente.controller.ts](src/paciente/paciente.controller.ts), + controllers de frequência/função/localidade/modalidade/programa/status-eventos (R1) | Autorização por tag (rodada 4) cobria só usuários/grupo-permissão/baixa/paciente(exclusão); agenda e os demais cadastros auxiliares seguiam só com `AuthGuard('jwt')`, embora o catálogo já tivesse tags específicas pra eles | `POST/PUT/DELETE /evento` e `POST/PUT/DELETE` dos 6 cadastros (frequência, função, localidade, modalidade, programa, status-eventos) mais `POST/PUT /paciente` ganharam `@RequirePermission` com a tag correspondente do catálogo real — mesmo critério de rigor da rodada 4 (só onde o mapeamento é inequívoco). `PUT /evento/check`/`/evento/atestado` e `/vagas/*` continuam sem tag — ação não mapeia 1:1 pra uma tag específica. Periodo/perfil/status/tipo-sessão/convênio/sessão/especialidade não têm tag no catálogo — impossível estender sem criar tag nova, que não é decisão minha tomar sozinha |
| 51 | [src/util/assert-not-in-use.ts](src/util/assert-not-in-use.ts) (novo), `delete()` de Especialidade/Função/Localidade/StatusEventos (R20) | Hard delete direto — se a entidade estivesse em uso, o Prisma rejeitava com erro cru de FK (500 genérico, mensagem incompreensível) | Helper reutilizável checa uso (via `count()` nas tabelas relacionadas) antes de excluir e lança erro com mensagem clara quando encontra. Continua sendo hard delete quando **não** está em uso — não virou desativação automática |
| 52 | [src/auth/jwt.strategy.ts](src/auth/jwt.strategy.ts), [src/main.ts](src/main.ts) | Fallback hardcoded `'dev-secret-key'` quando `JWT_PRIVATE_KEY` não definido, tanto no JWT quanto na sessão (que além disso reusava a chave errada — `SESSION_PRIVATE_KEY` já existia no `.env`, dedicada, e nunca era lida) | `JwtStrategy` lança erro no construtor se `JWT_PRIVATE_KEY` não estiver definido; `resolveSessionSecret()` (novo, exportado de `main.ts` pra ser testável) usa `SESSION_PRIVATE_KEY`, cai pra `JWT_PRIVATE_KEY` só se a primeira não existir, e lança erro se nenhuma das duas existir — nunca mais um valor fixo |
| 53 | `package.json` | README de migrations já referenciava `npm run migrate:deploy`, script que não existia | Adicionado `"migrate:deploy": "prisma migrate deploy"` |

**Fora do escopo desta rodada, deixado documentado e não tratado:** dedupe de `Paciente.carteirinha` (dados reais duplicados, decisão de limpeza pendente — §11), `RECEPCAO`/`RECEPCAO BASICO` com as mesmas tags perigosas que `ADM` (dado, não bug de código, decisão sua pendente — §11 item 21), rotação de credenciais expostas no histórico do git (ação sua, fora do meu acesso — §11 item 20), lint/prettier e substituição total de `console.log` por logger (baixo valor, alto diff, não priorizado), remoção de `express-session`/`passport.session()` (não investigado a fundo se o frontend depende de cookie de sessão em algum lugar), multiclínica (`Clinica`/tenant — feature nova, não um bug a corrigir).

---

## 10. Plano de correção (etapas restantes)

Numeração e prioridades mantidas do diagnóstico original. Quase tudo que estava listado como pendente até a rodada 5 foi concluído na rodada 6 — o que resta agora é essencialmente decisão de negócio ainda não tomada (§11) ou ação que só você pode fazer (rotação de credencial).

1. ~~**Segurança e integridade**~~ (Prioridade: Máxima) — ✅ **concluído**: identidade via JWT, status/mensagem reais de erro, guard de permissão por tag (agenda + demais cadastros, rodada 6), reset de senha seguro (senha aleatória + troca obrigatória), fim do mass assignment de `grupoPermissaoId` (rota separada) e do de `perfilId=Developer` (guard próprio). Resíduo: tags não existem no catálogo para periodo/perfil/status/tipo-sessão/convênio/sessão/especialidade (não é algo que eu resolva sem criar tag nova) e dado de `RECEPCAO`/`RECEPCAO BASICO`=`ADM` segue pendente de decisão (§11 item 21).
2. ~~**Cadastros e relacionamentos**~~ (Alta) — ✅ **concluído**: jornada 8h–20h validada, `perfilId` validado contra a tabela + enum de código com a lista real de perfis, exclusão condicionada a uso em Especialidade/Função/Localidade/StatusEventos, `@unique` em nomes de cadastro (StatusEventos/Especialidade/Função) onde os dados reais permitiam. Resíduo: `Paciente.carteirinha` sem `@unique` (dados reais duplicados, decisão de limpeza pendente); `Perfil` continua tabela livre, não virou enum de coluna no banco; `ID_PERFIL_TERAPEUTA` continua ID mágico.
3. ~~**Agenda**~~ — ✅ **concluído**: conflito de horário, jornada, vínculo especialidade/terapeuta/função, trava de campos pós-criação, bloqueio de evento passado (com Atestado + tolerância de 2h), rota `/especialidade`, exclusão de série bloqueada com sessão já realizada (rodada 6), autorização por tag (rodada 6) — tudo implementado e testado.
4. ~~**Recorrência**~~ — ✅ **concluído**: "esta e as próximas" divide a série corretamente sem tocar o passado; exclusão de série bloqueada se tiver sessão já realizada (rodada 6). "Exigir fim de recorrência" **não se aplica** — decisão de negócio confirmada de que série sem fim é o comportamento esperado.
5. ~~**Filas**~~ (Alta) — ✅ **concluído**: `switch`/fallthrough corrigido (rodada 1); `VagaService.update()` inteiro dentro de `prisma.$transaction` real, com o client propagado por `PacienteService` (rodada 6) — atomicidade de verdade, não mais `Promise.all` solto.
6. ~~**Baixas**~~ (Média-alta) — ✅ **concluído**: duplicidade sinalizada, `@@unique(eventoId)` a nível de banco, exclusão exige motivo e grava snapshot em `BaixaExclusaoLog` antes de apagar, update do evento + criação da baixa numa transação real (rodada 6).
7. ~~**Financeiro**~~ — ✅ **concluído**: `Decimal(10,2)` no banco real, valor/km e devolutiva configuráveis por env var, e agora **snapshot** (rodada 6): valor/comissão/tarifas congelados no evento, `FinanceiroService` prefere o snapshot ao cadastro atual, backfill retroativo nos eventos que já existiam.
8. **Testes** (Alta, em paralelo) — ⚠️ **parcial**: 119 testes hoje (era 31 no diagnóstico original), 88 novos ao longo das 6 rodadas, 18 suites. Ainda faltam: e2e, concorrência real (duas requisições simultâneas contra o banco, não só a lógica dentro de `$transaction`), isolamento entre clínicas (N/A, não existe `Clinica`) — cobrindo os cenários mínimos do enunciado.
9. ~~**Performance**~~ (Média) — ✅ **concluído**: teto de `pageSize` (100) em todos os endpoints paginados; índices em `Calendario` (`terapeutaId+start`, `pacienteId`, `groupId`) e `Baixa` (`usuarioId`, `dataEvento`) — rodada 6.
10. ~~**Observabilidade**~~ — ✅ **concluído**: log estruturado de toda requisição/erro via interceptor + filtro globais (`LoggingInterceptor`, `AllExceptionsFilter`). Não inclui integração com ferramenta de APM externa (Sentry/Datadog/etc.) nem substituição de todo `console.log` disperso nos services.
11. ~~**Migrations/Seeds**~~ (Alta) — ✅ **concluído**: seed de 18 tabelas de cadastro real, idempotente; `prisma migrate` adotado com baseline + 2 migrations reais; script `migrate:deploy` cadastrado em `package.json` (rodada 6).

---

## 11. Decisões de negócio pendentes

### Resolvidas nesta sessão

1. ~~Recorrência sem data final — precisa de regra de encerramento.~~ **Resolvido**: é o comportamento esperado (paciente ainda em atendimento); não é tratado como erro, nenhuma trava foi adicionada.
2. ~~Significado exato de "um dia de antecedência".~~ **Resolvido**: 48 horas corridas até o início do evento.
3. ~~Edição de paciente/especialidade/terapeuta/função/local em evento já existente — quais campos podem mudar.~~ **Resolvido**: paciente, especialidade, terapeuta, função, local, status e observação podem mudar; modalidade, data, horário, frequência, intervalo e dias da semana nunca mudam (mantidos do original, silenciosamente).
4. ~~Autorização por tag agora ou depois.~~ **Resolvido**: implementada nas rotas de maior risco assim que o catálogo real de tags apareceu no seed (rodada 4) — usuários, grupo-permissão, baixa, exclusão de paciente. Estendida na rodada 6 pra agenda e os 6 cadastros auxiliares com tag disponível.
5. ~~Lista exata de status permitidos para evento passado.~~ **Resolvido**: apenas **Atestado**. Implementado em `assertStatusPermitidoParaEventoPassado` ([src/agenda/agenda.service.ts](src/agenda/agenda.service.ts)) — qualquer outra tentativa de mudança de status em evento já ocorrido (Atendido, Falta, Cancelado com/sem Antecedência etc.) é rejeitada.
6. ~~Conflito dessa regra com o check-in mobile (`PUT /evento/check`, marca Atendido).~~ **Resolvido**: definida uma tolerância de **2 horas após o horário final** do evento durante a qual ele ainda não é considerado "passado" — dentro dessa janela, o evento se comporta normalmente (qualquer status, inclusive Atendido, pode ser aplicado). Só depois de 2h do término é que a trava "somente Atestado" passa a valer. Implementado em `isEventoPassado` via a constante `TOLERANCIA_EVENTO_PASSADO_HORAS`.
7. ~~Entrega da nova senha no reset, já que o sistema não tem e-mail/SMS.~~ **Resolvido**: senha aleatória, com troca obrigatória no primeiro login (`Usuario.mustChangePassword`) — bloqueia qualquer rota com tag até o usuário trocar. Implementado em [src/user/user.service.ts](src/user/user.service.ts) (rodada 6).
8. ~~Regra de quem pode atribuir grupo/perfil sensível (evitar autopromoção).~~ **Resolvido**: rota separada (`PUT /usuarios/:id/grupo-permissao`), guardada pela tag de administrar grupo de permissão — `POST/PUT /usuarios` não aceita mais `grupoPermissaoId`. Atribuir perfil Developer exige que quem chama já seja Developer (rodada 6).
9. ~~Granularidade de exclusão de série recorrente.~~ **Resolvido**: sem "só esta"/"esta e as próximas" — a regra fechada foi mais restritiva: bloquear a exclusão da série inteira se **qualquer** ocorrência dela já tiver sido realizada, permitindo excluir só séries 100% futuras (rodada 6).
10. ~~Congelar snapshot financeiro ou permitir reprocessamento.~~ **Resolvido**: congelar no evento (paciente/especialidade/terapeuta/função) e popular retroativamente os registros que já existiam com o melhor valor disponível hoje (rodada 6).

### Decisão tomada por mim nesta sessão (avise se não era o que você queria)

11. **Escopo do seed** ([prisma/seed.ts](prisma/seed.ts)) — você pediu "todos os dados do banco atual, exceto agendamento e filas". Além disso, **excluí `Usuario`, `Terapeuta`, `TerapeutaOnFuncao`, `Paciente` e `PacienteHistorico`** por conta própria: são dados de pessoas reais (nome, telefone, hash de senha de login) e colocar isso num arquivo `.ts` versionado no git exporia essa informação permanentemente no histórico do repositório — o mesmo tipo de problema que já tínhamos com o `.env` (§4 Altos). Se você realmente quer usuários/terapeutas/pacientes no seed, me avise explicitamente; não vou incluir PII de novo sem confirmação, mesmo que peça "todos os dados".
12. **Dedupe de `Paciente.carteirinha` não forçado** — o banco real tem duplicidade hoje (4 pares de carteirinha repetida + um placeholder `"-"` usado 22 vezes). Optei por **não** adicionar `@unique` (quebraria a migration) nem mexer nos dados existentes sozinha (poderia ser dado legítimo, tipo irmãos no mesmo plano, ou erro de cadastro — não é uma decisão técnica). Fica pendente de você revisar os 4 pares e decidir o que fazer antes de eu poder aplicar a constraint.

### Ainda pendentes

13. Critério exato para "avaliação concluída" (agendado vs. realizado; "~3 atendimentos" é limite rígido ou informativo?) — hoje `VagaService.verifyInFila` considera concluído quando todas as especialidades estão com `agendado=true`, não quando a sessão foi de fato realizada.
14. Relação entre Baixa e status "Atendido" — baixa deve existir para todo status cobrável (comportamento atual) ou só para "Atendido"?
15. Quem pode excluir/editar evento criado por outro usuário (hoje só o criador consegue excluir — comportamento inalterado).
16. Rotação de credenciais reais expostas no histórico do git (`.env`: banco, JWT, sessão) — preciso que você faça isso na sua ponta (não tenho acesso ao painel da hospedagem); ver §4 Altos.
17. **`RECEPCAO` e `RECEPCAO BASICO` têm hoje as mesmas tags perigosas que `ADM`** no banco real (resetar senha de terceiro, criar/editar grupo de permissão, editar/excluir usuário) — descoberto ao implementar o `PermissionsGuard` (§9 rodada 4), ainda verdade depois da rodada 6. É dado de `GrupoPermissaoOnPermissao`, não bug de código; o guard só *aplica* o que já está configurado. Se não for intencional, precisa de uma decisão sua sobre quais tags cada grupo deveria ter — não mexi nisso sozinha.
18. Multiclínica (`Clinica`/tenant) — definir se é requisito futuro; não é um bug, é ausência de uma feature que pode nunca ter sido pedida.
19. **`tati.granado` (Secretária) segue sem conseguir logar** — movida pro grupo `RECEPCAO` a seu pedido, mas esse grupo não tem tag `DEVICE_WEB`/`DEVICE_MOBILE` no banco real. Falta decidir: dar `DEVICE_WEB` pro grupo `RECEPCAO` (afeta todo mundo desse grupo, não só ela) ou mover ela pra outro grupo que já tenha a tag (ex.: `RECEPCAO BASICO`, que tem `DEVICE_WEB`). Ver nota pós-rodada 6 em §9 item 38.

---

*Relatório gerado e atualizado por auditoria assistida. Seis rodadas de correção aplicadas e verificadas nesta mesma sessão (`tsc --noEmit`, `nest build`, `npx jest` — 119/119 passando, mais um boot real da aplicação pra confirmar que nenhum guard novo quebra a inicialização). A rodada 6 fechou os itens que ainda estavam "Crítico — não corrigido" (senha previsível, mass assignment, transação real em filas/baixa, snapshot financeiro, exclusão de série sem checar sessão realizada) e a maior parte dos "Alto"/"Médio" que sobravam (exclusão de baixa auditada, exclusão de cadastro em uso, segredos sem fallback, autorização por tag estendida). O que resta em §10/§11 depende de decisão de negócio ainda não tomada ou de ação que só você pode fazer (rotação de credencial, revisão de dado de permissão, limpeza de carteirinha duplicada).*
